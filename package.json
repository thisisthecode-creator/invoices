{
  "name": "invoice-generator",
  "version": "1.0.0",
  "description": "Modern invoice generator with professional UI/UX",
  "main": "index.html",
  "scripts": {
    "dev": "live-server --port=3000 --open=/index.html --watch=.",
    "start": "live-server --port=3000 --open=/index.html",
    "build": "echo 'Static build - no bundling needed'",
    "preview": "live-server --port=3001 --open=/index.html"
  },
  "keywords": ["invoice", "generator", "pdf", "business", "billing"],
  "author": "Invoice Generator",
  "license": "MIT",
  "devDependencies": {
    "live-server": "^1.2.2"
  },
  "engines": {
    "node": ">=14.0.0"
  }
}
